name: Build & Release

on:
  push:
    branches: [main]
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install go-winres
        run: go install github.com/tc-hib/go-winres@latest

      - name: Build binaries
        run: make build

      - name: Generate bundle manifest
        shell: bash
        run: go run ./cmd/gen-manifest dist/Core/ > dist/Core/.bundle-manifest.json

      - name: Package dist folder
        shell: bash
        run: |
          cd dist
          7z a -tzip ../DevNull.zip . \
            -xr!*_ed25519 \
            -xr!*_ed25519.pub \
            -xr!.bundle-version \
            -xr!Core/state \

      - name: Get short SHA
        id: sha
        shell: bash
        run: echo "short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Update rolling latest release
        if: github.ref == 'refs/heads/main'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Delete existing latest release+tag if present
          gh release delete latest --yes 2>/dev/null || true
          git tag -d latest 2>/dev/null || true
          git push origin :refs/tags/latest 2>/dev/null || true

          # Create new release with all assets
          git tag latest
          git push origin latest
          gh release create latest \
            DevNull.zip \
            dist/Core/DevNullServer.exe \
            dist/Core/DevNullClient.exe \
            dist/Core/PinggyHelper.exe \
            --title "Latest Build (${{ steps.sha.outputs.short }})" \
            --notes "Auto-built from main at ${{ github.sha }}.

          **Fresh install:** save and run [install.ps1](https://github.com/simonthoresen/DevNullCore/raw/main/install.ps1).
          **Existing install:** \`DevNullServer.ps1\` / \`DevNull.ps1\` auto-update binaries on each launch." \
            --latest

      - name: Create versioned release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            DevNull.zip \
            dist/Core/DevNullServer.exe \
            dist/Core/DevNullClient.exe \
            dist/Core/PinggyHelper.exe \
            --title "Release ${{ github.ref_name }}" \
            --generate-notes
